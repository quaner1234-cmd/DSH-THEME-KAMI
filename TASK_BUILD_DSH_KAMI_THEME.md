# TASK — Build a Kami-inspired theme for DeepSeek Harness

Date: 2026-09-10

## 任务目标

在这个仓库中直接完成一个可实际安装、可验证、适合提交到 DSH Skin Market 的 DeepSeek Harness 主题。

主题的视觉来源是：

- Kami: https://github.com/tw93/Kami
- DSH Skin Market: https://github.com/kingOfSoySauce/dsh-skin-market

这不是做一份设计提案，也不是只写 demo。目标是最终得到一个真实可用的 DSH WebUI 主题插件。

## 我们想要的效果

核心不是“换几个颜色”，而是把 Kami 的纸媒 / editorial 设计语言真正带进 DSH。

希望用户打开 DSH 后第一感觉是：

- 像在一张安静、克制、排版良好的纸面上工作，而不是普通 SaaS 聊天界面；
- 长时间阅读舒服，信息层级清楚；
- Thinking、Tool Call、Observation、Code、Agent 输出等 DSH 特有内容仍然容易快速扫描；
- 整体有明显的 Kami 气质，而不是另一个 Claude / Anthropic 仿制主题。

需要重点保留 Kami 的视觉识别：

- warm parchment canvas，核心背景可围绕 `#f5f4ed`；
- ink blue 作为核心强调色，围绕 `#1B365D`；
- serif/editorial typography；
- 大量留白、细边框、克制阴影；
- 温暖的灰阶，而不是冷灰；
- 正文、标题、代码、工具信息之间有清楚而安静的视觉层级；
- 不要把界面做成卡片堆砌，也不要过度圆角、渐变、玻璃拟态或高饱和装饰。

Kami 本身最新的设计实现、token、页面结构、交互细节，请你直接研究其仓库，不要只根据上面几条机械复刻。

## 一个重要区别

DSH Skin Market 里已经有多个 Claude / parchment / paper 类主题。

请主动研究这些现有实现，尤其是它们已经解决了哪些 DSH theme/plugin 接口问题，但不要把本项目做成 Terracotta / Claude 风格的换皮。

我们的辨识度应该来自：

**Kami 的 Ink Blue + Paper + Editorial hierarchy。**

如果你认为为了适配 DSH，某些 Kami 原始设计规则需要调整，可以调整。目标是“像 Kami 设计的 DSH”，不是逐像素复制 Kami 官网。

## 必须遵守的边界

1. 不修改 DeepSeek Harness 本体源码来实现主题；应尽量采用 DSH 正式支持或当前生态已经验证可行的 theme/plugin 方式。
2. 主题必须可关闭、可卸载，并尽可能完整恢复 DSH 原始界面。
3. 不破坏 DSH 原有功能、交互、可访问性和关键信息状态。
4. 不打包、复制或重新分发许可证不明确或不允许再分发的字体文件。
   - 特别不要把 TsangerJinKai02 字体文件直接放进本仓库或插件包。
   - 中文字体请优先使用系统字体或明确允许开源再分发的字体方案。
5. 不直接复制其他 DSH 主题的代码后仅改色；可以研究其 API、兼容方法和工程实现，再自行实现。
6. 若引用、改编 Kami 的 MIT 代码或模板，请保留必要的许可证 / attribution 信息。
7. 不为了“看起来更特别”牺牲可读性。工具失败、警告、成功、代码、选择状态等仍必须清楚可辨。

## 你的自主权与执行权

这项任务给你较大的自主权。

你可以自行：

- 调研 Kami 当前实现；
- 调研 DSH 官方仓库、当前插件 / theme API；
- 调研 DSH Skin Market 中成熟主题的工程结构和兼容做法；
- 决定插件架构、文件结构、命名、token 映射方式和组件覆盖范围；
- 创建、删除、重构本仓库中的文件；
- 安装必要依赖；
- 编写脚本、测试、预览、截图工具；
- 根据真实运行结果迭代视觉设计；
- 在不违反上面边界的情况下，对细节做自己的专业判断。

不要因为需求没有规定每一个像素就停下来询问。对于低风险、可逆、能通过实际运行验证的决定，直接自行决定并继续执行。

如果发现当前设想与 DSH 的真实技术结构冲突，不要硬做。先确认现实中的正确实现方式，然后选择更可靠的方案继续完成目标。

## 推荐工作方式

先快速理解现实，再进入实现：

1. 查看当前 DSH 最新版本和主题插件机制。
2. 查看 DSH Skin Market 中 2–4 个实现质量较好的完整主题，理解它们如何注册、覆盖 token、处理 light/dark、启停与兼容性。
3. 深入查看 Kami 当前设计 token、排版、间距、边框、字体、页面层级，而不是只看截图。
4. 确定一个最小但完整的第一版方案。
5. 尽快在真实 DSH WebUI 中跑起来。
6. 依据真实界面继续迭代，而不是只在代码层面猜视觉效果。
7. 完成后做安装、启用、禁用、重启、卸载和核心 UI 的实际验证。

## 验收标准

最终完成时，至少应满足：

- 可以按 README 中的步骤安装到 DSH；
- 启用后主要 WebUI 视觉明显变为 Kami-inspired 风格；
- 主题覆盖核心工作区，而不是只改背景和按钮颜色；
- 会话列表、主会话流、输入区、Markdown、代码块、Thinking / Tool 等主要 DSH UI 状态视觉协调；
- 关键状态有足够对比度；
- 不包含受限字体文件；
- 禁用 / 卸载后不会留下明显污染；
- 重启后行为符合 README 描述；
- 有真实 DSH 截图作为验证证据；
- README 清楚说明安装、启用、卸载、兼容版本、设计来源和许可证；
- 仓库结构已经达到可以进一步提交 DSH Skin Market 的程度。

如果 DSH 当前 API 存在限制，导致某一项无法合理实现，请在 README 或最终报告中明确写出实际限制，而不是假装完成。

## 完成后请汇报

任务完成后给出一个简短报告：

- 最终采用了什么实现方案；
- 主要做了哪些视觉和工程改动；
- 实际测试了什么；
- 截图 / 证据在哪里；
- 仍存在什么已知限制；
- 是否已经达到可提交 DSH Skin Market 的状态；
- 最终 commit SHA。

除非遇到确实无法安全判断、需要账号权限、或会造成不可逆外部影响的事情，否则请直接执行到可验证完成，不要只停在分析、计划或半成品。