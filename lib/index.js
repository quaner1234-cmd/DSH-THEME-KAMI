/**
 * dsh-kami-theme — host half.
 *
 * Intentionally inert: the Kami skin is a browser-only theme. Keeping this
 * entry point empty means the plugin never requests filesystem, network,
 * process, or web-server access from the harness.
 */
export const name = 'dsh-kami-theme'

export function apply() {}